package com.fundemtals.practice.world;

public interface Soundable {
    // no dynamic fields
    // no constructors
    // it has only signature of methods without implementation
    // it is not possible to create instance of interface

    // Class cat extend only
    // 1 class but implements multiple interfaces
    // interface can extend other interface
    // interface method can ONLY be PUBLIC

    // method that does not have implementation
    // calls abstract methods
    void doSound();
}
